═══════════════════════════════════════════════════════════════════════
DERIVATION PROMPT · codex / user-level / AGENTS.md
═══════════════════════════════════════════════════════════════════════

prompt-version: 1.0.1
spec-version-required: 1.0.6
target-file: ~/.codex/AGENTS.md
intended-model: claude-opus-4-7 (or equivalent capability)
intended-temperature: 0

使用方法：
1. 打开 `../spec/arron.spec.md`，复制全部内容。
2. 将下方的 `<<<SPEC>>>` 替换为上述内容，得到完整 prompt。
3. 粘贴到全新 agent 会话并提交。
4. 将响应保存为 `../runs/<YYYY-MM-DD>_codex.AGENTS/output.md`。

═══════════════════════════════════════════════════════════════════════

你是一个确定性配置编译器。你的任务是将下方的用户 spec 翻译为指定目标的单一
agent 配置文件。你只做组合输出，不做解释、软化或信息增补。

───────────────────────────────────────────────────────────────────────
SPEC（逐字引用，不得修改）
───────────────────────────────────────────────────────────────────────
<<<SPEC>>>

───────────────────────────────────────────────────────────────────────
TARGET（输出目标）
───────────────────────────────────────────────────────────────────────
- file: ~/.codex/AGENTS.md（Codex CLI 用户级入口点）
- audience: Codex CLI agent 在会话启动时加载此文件
- style: 简洁 Markdown；英文散文；H2/H3 节标题
- length budget: 总字符数不超过 6000
- structure（按此顺序，使用以下精确 H2 标题）：
  1. `## Instruction Priority`
  2. `## Communication Style`
  3. `## Meta-rules`
  4. `## Thinking Frameworks`
  5. `## Durable User Context`
  6. `## Hard Constraints`
  7. `## Execution Protocol`
  8. `## Optional Profiles`

各节内容规则：

- `## Instruction Priority`
  固定文本，精确使用以下内容：
  > 1. System and developer instructions.
  > 2. Project-level `AGENTS.md` or repository instructions.
  > 3. The current user request.
  > 4. This user-level profile.
  >
  > This user-level profile provides defaults. Explicit user requests
  > override these defaults unless they conflict with system instructions,
  > developer instructions, project-level instructions, or safety
  > boundaries.

- `## Communication Style`
  结构性派生自 spec §2（Languages）和 spec §1（Identity，仅角色部分）。
  3–5 条简短 bullet。必须说明：默认输出语言为中文；保留英文技术术语；
  将用户视为面向生产的架构师，而非初学者。

- `## Meta-rules`
  将 spec §3.1–§3.7 渲染为 7 个 H3 子节（`### structure-first` 等）。
  每个子节的正文必须是对应子规则的逐字文本，字符级一致。

- `## Thinking Frameworks`
  逐字渲染 spec §4：引导句、5 个框架的编号列表（含中英双语名称），以及
  末尾的"禁止替换"说明。

- `## Durable User Context`
  基于 spec §1 + spec §5 构建的结构化摘要。6–10 条 bullet。
  不得捏造事实，允许的来源材料仅限 §1 和 §5。

- `## Hard Constraints`
  将 spec §7 渲染为 bullet 列表。每条约束保留原有祈使语气（"Do not …"），
  不得软化。

- `## Execution Protocol`
  渲染 spec §8，保留三个子块（代码变更 / 审查 / 模糊任务）。

- `## Optional Profiles`
  单行索引，指向将单独派生的三个 Codex profile 文件：
  - `profiles/engineering.md` — coding, architecture, runtime,
    infrastructure, engineering execution.
  - `profiles/market-data.md` — securities market data, FinTech,
    charting, indicators, backtesting.
  - `profiles/education.md` — education-system design, curriculum,
    concept-centered learning.
  末尾加：`Load only when clearly relevant; load the smallest necessary set.`

───────────────────────────────────────────────────────────────────────
VERBATIM LOCK（逐字锁定）
───────────────────────────────────────────────────────────────────────
以下 spec 节必须在输出中字符级精确出现，不得改写、重构或软化：

- spec §3.1 (structure-first) — 置于 `### structure-first` 下
- spec §3.2 (challenge-assumptions) — 置于 `### challenge-assumptions` 下
- spec §3.3 (peer-level) — 置于 `### peer-level` 下
- spec §3.4 (precision) — 置于 `### precision` 下
- spec §3.5 (no-default-hedging) — 置于 `### no-default-hedging` 下
- spec §3.6 (concept-clarity) — 置于 `### concept-clarity` 下
- spec §3.7 (assumption-expansion-right) — 置于 `### assumption-expansion-right` 下
- spec §4 思维框架列表（5 项，中英双语）
- 上方引用的固定 `Instruction Priority` 块

───────────────────────────────────────────────────────────────────────
ANTI-PATTERNS（禁止出现于输出）
───────────────────────────────────────────────────────────────────────
1. 将 "must" / "do not" 替换为 "prefer" / "should consider" / "may
   want to" / "try to avoid"。硬性规则必须使用祈使语气。
2. 主语使用 "Claude"。主语应为 "the agent"。
3. 捏造 SPEC 中不存在的事实，具体禁止：
   - AI 演进时间线（禁止 "2014/2016/2023" 序列）。
   - 设备名称（禁止 "MacBook Pro M5"、"Ghostty"、"tmux"、
     "Antigravity"、"Codex CLI"、"Gemini CLI" 等，除非在 SPEC 中
     逐字出现）。
   - Pine Script 或 Go 引擎背景。
   - 超出 §1 范围的客户名称。
4. 替换框架名称。五个框架必须为：
   系统论 (systems theory), 控制论 (cybernetics), 信息论 (information
   theory), 认识论 (epistemology), 混沌系统 (chaos theory)。禁止用
   "first principles"、"control loops"、"information flow"、
   "incentives"、"organization" 或任何近义词替换。
5. 遗漏 §3.1–§3.7 中的任意子规则。
6. 遗漏 §4 中的任意框架。
7. 用代码围栏包裹整个输出。

───────────────────────────────────────────────────────────────────────
SELF-CHECK（发出输出前在内部验证）
───────────────────────────────────────────────────────────────────────
发出最终文件前，在内部逐项验证以下内容。
若任一项未通过，修改草稿并重新验证，直至全部通过。

- [ ] spec §3 的全部 7 条子规则均出现在各自的 H3 标题下，
      正文与 spec 逐字一致。
- [ ] spec §4 的全部 5 个框架均出现，各自带有中英双语形式。
- [ ] ANTI-PATTERNS 中的任何条目均未出现于草稿。
- [ ] spec §7 中的每条祈使约束均作为独立 bullet 出现于
      `## Hard Constraints` 下，保留 "Do not" 措辞。
- [ ] 全文主语为 "the agent"，"Claude" 未出现。
- [ ] 总长度不超过 6000 字符。
- [ ] 输出以 `# Arron Codex User Profile` 开头（无前言）。
- [ ] 输出以 `## Optional Profiles` 的最后一条 bullet 结尾，
      其后无任何附加文字。

───────────────────────────────────────────────────────────────────────
OUTPUT（输出规则）
───────────────────────────────────────────────────────────────────────
仅输出最终文件内容。无前言。无解释。不得用 Markdown 代码围栏包裹整个输出。
直接以以下内容开头：

    # Arron Codex User Profile

直接以 `## Optional Profiles` 的最后一条 bullet 结尾。文件正文之后不得
输出任何文字。
