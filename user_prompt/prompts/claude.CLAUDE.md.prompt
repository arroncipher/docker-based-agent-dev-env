═══════════════════════════════════════════════════════════════════════
DERIVATION PROMPT · claude / user-level / CLAUDE.md (thin entry, ≤1024B)
═══════════════════════════════════════════════════════════════════════

prompt-version: 2.0.0
spec-version-required: 1.0.5
target-file: ~/.claude/CLAUDE.md
intended-model: claude-sonnet-4-6 (or equivalent capability)
intended-temperature: 0

使用方法：
1. 打开 `../spec/arron.spec.md`，复制全部内容。
2. 将下方的 `<<<SPEC>>>` 替换为上述内容，得到完整 prompt。
3. 粘贴到全新 agent 会话并提交。
4. 将响应保存为 `../runs/<YYYY-MM-DD>_claude.CLAUDE.md/output.md`。

注意：实际写入路径为 `.coding_agents/claude_max20/.claude/CLAUDE.md`；
target-file 头部记录的是逻辑路径。

本 prompt 派生 thin entry-point（≤1024 字节硬上限）。仅包含 spec §2 与 §7。
§1/§3/§4/§5/§6/§8 由 `claude.rules.meta-rules.md.prompt` 与
`claude.rules.context.md.prompt` 派生到 `.claude/rules/`。

═══════════════════════════════════════════════════════════════════════

你是一个确定性配置编译器。你的任务是将下方的用户 spec 翻译为指定目标的单一
agent 配置文件。你只做组合输出，不做解释、软化或信息增补。

───────────────────────────────────────────────────────────────────────
SPEC（逐字引用，不得修改）
───────────────────────────────────────────────────────────────────────
<<<SPEC>>>

───────────────────────────────────────────────────────────────────────
TARGET（输出目标）
───────────────────────────────────────────────────────────────────────
- file: ~/.claude/CLAUDE.md（Claude Code 用户级入口文件）
- audience: Claude Code agent 在会话启动时加载此文件
- style: 简洁 Markdown；叙述性内容以简体中文（zh-CN）；节标题使用中文，
  硬性规则祈使语气保留英文
- length budget: 总字符数 **不得超过 1024 字节**（硬上限）
- structure（按此顺序，使用以下精确 H2 标题）：
  1. `## 语言与沟通风格`
  2. `## Hard Constraints`

各节内容规则：

- `## 语言与沟通风格`
  结构性派生自 spec §2（Languages）和 spec §1（仅角色信息）。
  2 条简短 bullet：
  - 首条说明默认输出语言为简体中文（zh-CN），技术术语保留英文。
  - 次条说明用户身份为面向生产的软件架构师与 AI-native 工程从业者，
    以同行视角交流。

- `## Hard Constraints`
  将 spec §7 渲染为 bullet 列表。每条约束保留原有祈使语气（"Do not …"）。
  不得软化。§7 的六条约束必须全部出现。允许去除括号内说明性短语
  （例如末尾的 "(no device names, no AI-evolution timelines, …)"）
  以节省字节预算，但禁止改写约束本体。

───────────────────────────────────────────────────────────────────────
VERBATIM LOCK（逐字锁定）
───────────────────────────────────────────────────────────────────────
- spec §7 的 6 条 Hard Constraints 主体必须以英文祈使句形式逐字出现。
  允许移除条目末尾括号说明（仅 §7 最后一条的括号部分可省略）。

───────────────────────────────────────────────────────────────────────
ANTI-PATTERNS（禁止出现于输出）
───────────────────────────────────────────────────────────────────────
1. 将 "must" / "do not" 替换为 "prefer" / "should consider" /
   "may want to" / "try to avoid"。硬性规则必须使用祈使语气。
2. 主语使用 "Claude"（"Claude must …"、"Claude should …"）。
   主语应为 "the agent"。
3. 捏造 SPEC 中不存在的事实（AI 演进时间线、设备名称、Pine Script /
   Go 引擎背景、超出 §1 的客户名称等）。
4. 在本文件内包含 spec §3 Meta-rules、§4 Thinking Frameworks、
   §5 Domains、§6 Active Projects、§8 Execution Protocol 任一内容
   ——这些必须留给 `claude.rules.*` 派生目标。
5. 用代码围栏包裹整个输出。
6. 输出超过 1024 字节硬上限。
7. 使用文件工具（Read、Write、Edit）写入磁盘——必须直接输出到 stdout，
   不得操作文件系统。

───────────────────────────────────────────────────────────────────────
SELF-CHECK（发出输出前在内部验证）
───────────────────────────────────────────────────────────────────────
发出最终文件前，在内部逐项验证以下内容。
若任一项未通过，修改草稿并重新验证，直至全部通过。

- [ ] 总字节数 ≤ 1024。
- [ ] 仅含 2 个 H2 节：`## 语言与沟通风格`、`## Hard Constraints`。
- [ ] spec §7 中的 6 条祈使约束（Do not …）均作为独立 bullet 出现，
      主体逐字一致。
- [ ] 全文主语为 "the agent"，"Claude" 未作为义务主语出现。
- [ ] 输出未包含 §3 / §4 / §5 / §6 / §8 任意内容。
- [ ] 输出以 `# Arron Claude User Profile` 开头（无前言）。
- [ ] 输出以 §7 最后一条 bullet 结尾，其后无任何附加文字。
- [ ] 未使用任何文件工具（Read / Write / Edit）——输出以纯文本形式打印到 stdout。

───────────────────────────────────────────────────────────────────────
OUTPUT（输出规则）
───────────────────────────────────────────────────────────────────────
仅输出最终文件内容。无前言。无解释。不得用 Markdown 代码围栏包裹整个输出。
直接以以下内容开头：

    # Arron Claude User Profile

直接以 `## Hard Constraints` 的最后一条 bullet 结尾。文件正文之后不得输出任何文字。
