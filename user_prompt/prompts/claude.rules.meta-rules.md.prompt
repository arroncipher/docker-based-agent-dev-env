═══════════════════════════════════════════════════════════════════════
DERIVATION PROMPT · claude / user-level / rules/meta-rules.md
═══════════════════════════════════════════════════════════════════════

prompt-version: 1.0.4
spec-version-required: 1.0.11
target-file: ~/.claude/rules/meta-rules.md
intended-model: claude-sonnet-4-6 (or equivalent capability)
intended-temperature: 0

使用方法：
1. 打开 `../spec/arron.spec.md`，复制全部内容。
2. 将下方的 `<<<SPEC>>>` 替换为上述内容，得到完整 prompt。
3. 粘贴到全新 agent 会话并提交。
4. 将响应保存为 `../runs/<YYYY-MM-DD>_claude.rules.meta-rules/output.md`。

注意：实际写入路径为 `.coding_agents/claude_max20/.claude/rules/meta-rules.md`。
此文件为 always-loaded rules（无 `paths:` frontmatter），优先级与
`CLAUDE.md` 相同。

═══════════════════════════════════════════════════════════════════════

你是一个确定性配置编译器。你的任务是将下方的用户 spec 中 §3（Meta-rules）
与 §4（Thinking Frameworks）逐字翻译为指定目标的 rules 文件。你只做组合
输出，不做解释、软化或信息增补。

───────────────────────────────────────────────────────────────────────
SPEC（逐字引用，不得修改）
───────────────────────────────────────────────────────────────────────
<<<SPEC>>>

───────────────────────────────────────────────────────────────────────
TARGET（输出目标）
───────────────────────────────────────────────────────────────────────
- file: ~/.claude/rules/meta-rules.md（Claude Code always-loaded rule）
- audience: Claude Code agent 在会话启动时与 CLAUDE.md 一并加载
- style: 简洁 Markdown；保留 spec 原文措辞；不得加入任何叙述性引言
- length budget: 总字符数不超过 5000
- frontmatter: **不得**包含 `paths:` 字段；本文件为常驻加载
- structure（按此顺序，使用以下精确 H2 标题）：
  1. `## Meta-rules`
  2. `## Thinking Frameworks`

各节内容规则：

- `## Meta-rules`
  将 spec §3.1–§3.13 渲染为 13 个 H3 子节，标题精确为：
  - `### structure-first`
  - `### challenge-assumptions`
  - `### peer-level`
  - `### precision`
  - `### no-default-hedging`
  - `### concept-clarity`
  - `### assumption-expansion-right`
  - `### abstraction-discipline`
  - `### framework-relevance`
  - `### review-design-separation`
  - `### convergence-discipline`
  - `### entity-binding`
  - `### inferential-validity`
  每个子节正文必须是对应子规则的逐字文本，字符级一致。

- `## Thinking Frameworks`
  逐字渲染 spec §4：引导句、5 个框架的编号列表（含中英双语名称），以及
  末尾的"禁止替换"说明。

───────────────────────────────────────────────────────────────────────
VERBATIM LOCK（逐字锁定）
───────────────────────────────────────────────────────────────────────
以下 spec 元素必须在输出中字符级精确出现，不得改写、重构、软化或压缩：

- spec §3.1 (structure-first) — 置于 `### structure-first` 下
- spec §3.2 (challenge-assumptions) — 置于 `### challenge-assumptions` 下
- spec §3.3 (peer-level) — 置于 `### peer-level` 下
- spec §3.4 (precision) — 置于 `### precision` 下
- spec §3.5 (no-default-hedging) — 置于 `### no-default-hedging` 下
- spec §3.6 (concept-clarity) — 置于 `### concept-clarity` 下
- spec §3.7 (assumption-expansion-right) — 置于
  `### assumption-expansion-right` 下
- spec §3.8 (abstraction-discipline) — 置于
  `### abstraction-discipline` 下
- spec §3.9 (framework-relevance) — 置于
  `### framework-relevance` 下
- spec §3.10 (review-design-separation) — 置于
  `### review-design-separation` 下
- spec §3.11 (convergence-discipline) — 置于
  `### convergence-discipline` 下
- spec §3.12 (entity-binding) — 置于
  `### entity-binding` 下
- spec §3.13 (inferential-validity) — 置于
  `### inferential-validity` 下
- spec §4 思维框架编号列表（5 项，中英双语，按 spec 给定顺序）
- spec §4 引导句及末尾"禁止替换"说明

───────────────────────────────────────────────────────────────────────
ANTI-PATTERNS（禁止出现于输出）
───────────────────────────────────────────────────────────────────────
1. 将 "必须" / "不得" 替换为 "建议" / "应考虑" / "尽量避免" 等弱化表述。
2. 主语使用 "Claude"。主语应为 "the agent"。
3. 替换框架名称。五个框架必须为：
   系统论 (systems theory), 控制论 (cybernetics), 信息论 (information
   theory), 认识论 (epistemology), 混沌系统 (chaos theory)。禁止用
   "first principles"、"control loops"、"information flow"、
   "incentives"、"organization" 或任何近义词替换。
4. 遗漏 §3.1–§3.13 中的任意子规则（含 §3.8–§3.13 新增规则）。
5. 遗漏 §4 中的任意框架。
6. 在文件开头加入 YAML frontmatter（`---` 块）。本文件为常驻 rule，
   无需 frontmatter。
7. 添加 spec 未指定的 H2 节（例如 `## 用户背景`、`## Hard Constraints`、
   `## Execution Protocol`、`## 活跃项目`）——这些属于其他派生目标。
8. 用代码围栏包裹整个输出。
9. 使用文件工具（Read、Write、Edit）写入磁盘——必须直接输出到 stdout，
   不得操作文件系统。

───────────────────────────────────────────────────────────────────────
SELF-CHECK（发出输出前在内部验证）
───────────────────────────────────────────────────────────────────────
发出最终文件前，在内部逐项验证以下内容。
若任一项未通过，修改草稿并重新验证，直至全部通过。

- [ ] spec §3 的全部 13 条子规则均出现在各自的 H3 标题下（§3.1
      structure-first 至 §3.13 inferential-validity），正文与 spec 逐字一致。
- [ ] spec §4 的全部 5 个框架均出现，各自带有中英双语形式，
      按 spec §4 所列顺序排列。
- [ ] §4 结尾保留短语 "在派生目标中"。
- [ ] 文件以 `## Meta-rules` 开头（无 H1、无 frontmatter、无前言）。
- [ ] 文件以 §4 末尾 "禁止替换" 说明的最后一行结尾，其后无任何附加文字。
- [ ] 总长度不超过 5000 字符。
- [ ] 输出仅含 2 个 H2 节：`## Meta-rules`、`## Thinking Frameworks`。
- [ ] 未使用任何文件工具（Read / Write / Edit）——输出以纯文本形式打印到 stdout。

───────────────────────────────────────────────────────────────────────
OUTPUT（输出规则）
───────────────────────────────────────────────────────────────────────
仅输出最终文件内容。无前言。无解释。不得用 Markdown 代码围栏包裹整个输出。
直接以以下内容开头：

    ## Meta-rules

直接以 spec §4 末尾"禁止替换"说明结尾。文件正文之后不得输出任何文字。
