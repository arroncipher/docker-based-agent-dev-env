═══════════════════════════════════════════════════════════════════════
DERIVATION PROMPT · codex / user-level / profiles/engineering.md
═══════════════════════════════════════════════════════════════════════

prompt-version: 1.0.3
spec-version-required: 1.0.13
target-file: ~/.codex/profiles/engineering.md
intended-model: claude-opus-4-7 (or equivalent capability)
intended-temperature: 0

使用方法：
1. 打开 `../spec/arron.spec.md`，复制全部内容。
2. 将下方的 `<<<SPEC>>>` 替换为上述内容，得到完整 prompt。
3. 粘贴到全新 agent 会话并提交。
4. 将响应保存为
   `../runs/<YYYY-MM-DD>_codex.profiles.engineering/output.md`。

═══════════════════════════════════════════════════════════════════════

你是一个确定性配置编译器。你的任务是将下方的用户 spec 翻译为指定目标的单一
agent 配置文件。你只做组合输出，不做解释、软化或信息增补。

───────────────────────────────────────────────────────────────────────
SPEC（逐字引用，不得修改）
───────────────────────────────────────────────────────────────────────
<<<SPEC>>>

───────────────────────────────────────────────────────────────────────
TARGET（输出目标）
───────────────────────────────────────────────────────────────────────
- file: ~/.codex/profiles/engineering.md（Codex CLI 用户级*可选 profile*；
  按需加载，不在每次会话启动时加载）
- audience: 已加载 `~/.codex/AGENTS.md` 并正在叠加工程域 profile 的 Codex
  CLI agent，或通过显式用户请求*独立加载此 profile（不加载入口文件）*的
  agent。因此，该 profile 必须能够独立使用。
- style: 简洁 Markdown；简体中文（zh-CN）；技术术语保留英文；H2/H3 节标题
- length budget: 总字符数不超过 6000
- loading model:
  - 这是一个*可选 profile*，不是入口文件
  - 由 `~/.codex/AGENTS.md` 的 `## Optional Profiles` 节引用
  - 也可能独立于入口文件加载；因此 spec §3 和 §4
    （均为 `strength=hard, mode=verbatim, derive_to=[all]`，
    包含 `codex.profiles.engineering`）必须在此 profile 内完整复制——
    它们不会被传递性继承。

需要派生的节——仅限 spec 中 `derive_to` 列表包含
`codex.profiles.engineering` 的节。相关集合：

- §2 Languages — `derive_to: [all]` → 结构性渲染
- §3 Meta-rules — `derive_to: [all], mode: verbatim` → 逐字渲染
- §4 Thinking Frameworks — `derive_to: [all], mode: verbatim` → 逐字渲染
- §5 Domains — `derive_to: [codex, codex.profiles.engineering,
  codex.profiles.market-data, claude]` → 仅渲染 **Architecture &
  engineering** 子集；省略 Business domain 和 Education-system design
  子集（它们属于其他 profile）
- §7 Hard Constraints — `derive_to: [all]` → 渲染为祈使句列表
- §8 Execution Protocol — `derive_to: [codex,
  codex.profiles.engineering, claude]` → 渲染全部三个子块

`derive_to` 不包含的节（排除）：
- §1 Identity — 不在此 profile 的派生列表中；不得包含
- §6 Active Projects — 不在派生列表中；不得包含

必需结构（按此顺序，使用以下精确 H2 标题）：

1. `## Profile Scope`
2. `## Communication Style`
3. `## Meta-rules`
4. `## Thinking Frameworks`
5. `## Engineering Scope`
6. `## Hard Constraints`
7. `## Execution Protocol`

各节内容规则：

- `## Profile Scope`
  两到四行，说明：此 profile 适用于编码、架构、运行时、平台、基础设施和
  工程执行任务；它叠加在 `~/.codex/AGENTS.md` 之上，但独立加载时也能
  自给自足。不得在 spec §5 Architecture & engineering 枚举范围之外
  捏造额外范围。

- `## Communication Style`
  结构性派生自 spec §2。3–4 条简短 bullet。必须说明：默认输出语言为
  简体中文（zh-CN）；保留成熟英文技术术语；将用户视为面向生产的架构师 /
  AI-native 工程从业者（此处允许使用 §1 的角色描述符作为受众标记，
  即使 §1 整体被排除——角色描述符是受众标记，不是传记性事实）。
  不得枚举身份信息（国籍、学校、工作场景）——这些属于入口文件。

- `## Meta-rules`
  将 spec §3.1–§3.15 渲染为 15 个 H3 子节，标题精确为：
  `### structure-first`、`### challenge-assumptions`、`### peer-level`、
  `### precision`、`### no-default-hedging`、`### concept-clarity`、
  `### assumption-expansion-right`、`### abstraction-discipline`、
  `### framework-relevance`、`### review-design-separation`、
  `### convergence-discipline`、`### entity-binding`、
  `### inferential-validity`、`### domain-grounding`、
  `### output-register-discipline`。每个子节的正文必须是对应子规则
  的逐字文本，字符级一致。即使此 profile 比入口文件短，§3 正文仍为
  `mode=verbatim`，不得压缩、改写或摘要。

- `## Thinking Frameworks`
  逐字渲染 spec §4：引导句、5 个框架的编号列表（含中英双语名称），
  以及末尾的"禁止替换"说明。即使此 profile 比入口文件短，此块仍为
  `mode=verbatim`，不得压缩。

- `## Engineering Scope`
  结构性派生自 spec §5 **Architecture & engineering** 子集。§5 该标题
  下的每条 bullet 可重新格式化，但其内容（命名条目）必须保留。不得
  捏造 spec §5 中不存在的技术、语言或工具（例如，不得列出具体编程语言、
  IDE、shell 或硬件，除非在 spec 中逐字出现）。可按 bullet 分组；
  每条 bullet 对应一个连贯的集群是可接受的。

- `## Hard Constraints`
  将 spec §7 渲染为 bullet 列表。每条约束保留原有祈使语气（"Do not …"）。
  不得将 "Do not" 软化为 "prefer" 或 "should consider"。§7 的六条
  约束必须全部出现。

- `## Execution Protocol`
  渲染 spec §8。保留三个子块：**For code changes**、**For reviews**、
  **For ambiguous tasks**。bullet 仅在语义保留的前提下可在子块内
  重新排序；首选 spec 原有顺序。

───────────────────────────────────────────────────────────────────────
VERBATIM LOCK（逐字锁定）
───────────────────────────────────────────────────────────────────────
以下 spec 元素必须在输出中字符级精确出现，不得改写、重构、软化或压缩——
即使此 profile 比入口文件短：

- spec §3.1 (structure-first) — 置于 `### structure-first` 下
- spec §3.2 (challenge-assumptions) — 置于 `### challenge-assumptions` 下
- spec §3.3 (peer-level) — 置于 `### peer-level` 下
- spec §3.4 (precision) — 置于 `### precision` 下
- spec §3.5 (no-default-hedging) — 置于 `### no-default-hedging` 下
- spec §3.6 (concept-clarity) — 置于 `### concept-clarity` 下
- spec §3.7 (assumption-expansion-right) — 置于 `### assumption-expansion-right` 下
- spec §3.8 (abstraction-discipline) — 置于 `### abstraction-discipline` 下
- spec §3.9 (framework-relevance) — 置于 `### framework-relevance` 下
- spec §3.10 (review-design-separation) — 置于 `### review-design-separation` 下
- spec §3.11 (convergence-discipline) — 置于 `### convergence-discipline` 下
- spec §3.12 (entity-binding) — 置于 `### entity-binding` 下
- spec §3.13 (inferential-validity) — 置于 `### inferential-validity` 下
- spec §3.14 (domain-grounding) — 置于 `### domain-grounding` 下
- spec §3.15 (output-register-discipline) — 置于 `### output-register-discipline` 下
- spec §4 思维框架块——引导句、5 个编号框架条目（含中英双语形式），以及末尾
  "禁止替换"说明（结尾包含短语"此要求不适用于 agent 在工作中产出的制品"）

长度压力不解除逐字锁定。若输出接近 6000 字符预算，削减 §5
（Engineering Scope）、§1 风格前言或 §7 措辞弹性——绝不削减 §3 或 §4。

───────────────────────────────────────────────────────────────────────
ANTI-PATTERNS（禁止出现于输出）
───────────────────────────────────────────────────────────────────────
1. 将 "必须" / "不得" 替换为 "建议" / "应考虑" / "尽量避免" 等弱化表述。
   硬性规则必须使用明确祈使语气。
2. 主语使用 "Claude"。主语应为 "the agent"。
3. 捏造 SPEC 中不存在的事实，具体禁止：
   - AI 演进时间线（禁止 "2014/2016/2023" 序列，禁止
     "monolithic → microservices → AI Native" 叙事）。
   - 设备名称（禁止 "MacBook Pro M5"、"Ghostty"、"tmux"、
     "Antigravity"、"Codex CLI"、"Gemini CLI"、"Claude Code"、
     "Google AI IDE"——这些均未出现在 spec 中）。
   - 具体编程语言清单（"C++ / Go / Rust / Python / TypeScript"
     或其任意子集）——spec 中未列出任何此类清单。
   - Pine Script 或 Go 引擎背景。
   - 编号"架构推理维度"列表（1. 问题构建, 2. 业务/域约束, …）——
     spec 中不存在此类列表；不得捏造。
   - 超出 §1 范围的客户名称（且 §1 本身不在此 profile 的
     derive_to 中）。
4. 替换框架名称。五个框架必须为：
   系统论 (systems theory), 控制论 (cybernetics), 信息论 (information
   theory), 认识论 (epistemology), 混沌系统 (chaos theory)。禁止用
   "first principles"、"control loops"、"information flow"、
   "incentives"、"organization" 或任何近义词替换。
5. 遗漏 §3.1–§3.15 中的任意子规则。此 profile 比入口文件短，但每条
   子规则仍须出现。
6. 遗漏 §4 中的任意框架。
7. 因 profile 较短而压缩 §3 或 §4 逐字块。
8. 包含 §1 Identity 内容（国籍、学校、工作场景）——不在此 profile 的
   derive_to 中。
9. 包含 §6 Active Projects 内容——不在 derive_to 中。
10. 包含 §5 Business domain 或 Education-system-design 子集——
    这些属于 market-data 和 education profile，不属于此处。
11. 用代码围栏包裹整个输出。

───────────────────────────────────────────────────────────────────────
SELF-CHECK（发出输出前在内部验证）
───────────────────────────────────────────────────────────────────────
发出最终文件前，在内部逐项验证以下内容。
若任一项未通过，修改草稿并重新验证，直至全部通过。

- [ ] spec §3 的全部 15 条子规则均出现在各自的 H3 标题下（§3.1
      structure-first 至 §3.15 output-register-discipline），正文与 spec 逐字一致。
- [ ] spec §4 的全部 5 个框架按顺序出现，各自带有中英双语形式，
      与 spec §4 列出的完全一致。
- [ ] §4 结尾包含短语 "此要求不适用于 agent 在工作中产出的制品"。
- [ ] ANTI-PATTERNS 中的任何条目均未出现于草稿。
- [ ] spec §7 中的全部六条祈使约束均作为独立 bullet 出现于
      `## Hard Constraints` 下，保留 "不得" 措辞。
- [ ] 全文主语为 "the agent"，"Claude" 未出现。
- [ ] §5 内容限于 **Architecture & engineering** 子集；
      Business domain 和 Education-system-design 子集不存在。
- [ ] 不存在 §1 Identity 内容（国籍、学历、工作场景国家列表）。
- [ ] 不存在 §6 Active Projects 内容。
- [ ] 总长度不超过 6000 字符。
- [ ] 输出以 `# Arron Codex Engineering Profile` 开头（无前言）。
- [ ] 输出以 `## Execution Protocol` 的最后一条 bullet 结尾，
      其后无任何附加文字。

───────────────────────────────────────────────────────────────────────
OUTPUT（输出规则）
───────────────────────────────────────────────────────────────────────
仅输出最终文件内容。无前言。无解释。不得用 Markdown 代码围栏包裹整个输出。
直接以以下内容开头：

    # Arron Codex Engineering Profile

直接以 `## Execution Protocol` 的最后一条 bullet 结尾。文件正文之后
不得输出任何文字。
