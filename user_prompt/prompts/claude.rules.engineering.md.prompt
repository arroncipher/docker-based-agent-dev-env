═══════════════════════════════════════════════════════════════════════
DERIVATION PROMPT · claude / user-level / rules/engineering.md
═══════════════════════════════════════════════════════════════════════

prompt-version: 1.0.1
spec-version-required: 1.0.10
target-file: ~/.claude/rules/engineering.md
intended-model: claude-sonnet-4-6 (or equivalent capability)
intended-temperature: 0

使用方法：
1. 打开 `../spec/arron.spec.md`，复制全部内容。
2. 将下方的 `<<<SPEC>>>` 替换为上述内容，得到完整 prompt。
3. 粘贴到全新 agent 会话并提交。
4. 将响应保存为 `../runs/<YYYY-MM-DD>_claude.rules.engineering/output.md`。

注意：实际写入路径为 `.coding_agents/claude_max20/.claude/rules/engineering.md`。
此文件为 always-loaded rule（无 `paths:` frontmatter），覆盖
Architecture & engineering + Business domain 两个子域——两者对用户
的工程实践不可分离，统一常驻加载。

═══════════════════════════════════════════════════════════════════════

你是一个确定性配置编译器。你的任务是将下方的用户 spec 中 §5（Domains）
的 Architecture & engineering 与 Business domain 两个子集合并翻译为
always-loaded rule 文件。你只做组合输出，不做解释、软化或信息增补。

───────────────────────────────────────────────────────────────────────
SPEC（逐字引用，不得修改）
───────────────────────────────────────────────────────────────────────
<<<SPEC>>>

───────────────────────────────────────────────────────────────────────
TARGET（输出目标）
───────────────────────────────────────────────────────────────────────
- file: ~/.claude/rules/engineering.md（Claude Code always-loaded rule）
- audience: Claude Code agent 在会话启动时与 CLAUDE.md 一并加载
- style: 简洁 Markdown；叙述性内容以简体中文（zh-CN）；技术术语保留
  英文；不得加入任何引言
- length budget: 总字符数不超过 3500
- frontmatter: **不得**包含 `paths:` 字段；本文件为常驻加载
- structure（按此顺序，使用以下精确 H2 标题）：
  1. `## Architecture & Engineering`
  2. `## Market-Data 域上下文`
  3. `## 工作模式`

各节内容规则：

- `## Architecture & Engineering`
  基于 spec §5 中的 "Architecture & engineering" 子集（且仅该子集）
  渲染。以中文撰写 4–6 条 bullet，覆盖：
  - 单体 / 分布式 / cloud-native 经验
  - DevOps、平台工程、harness 工程
  - agentic 软件工程、host-agent、sandbox-executor、tool-boundary
  - AI Native 产品策略、workflow 重构
  - 开发者工具、可观测性、生产运维
  - 早期背景：嵌入式 + 图像处理算法优化
  允许将多个相关项合并为单一 bullet 以控制长度。不得引入 §5 中不存在
  的子项。

- `## Market-Data 域上下文`
  基于 spec §5 中的 "Business domain" 子集（且仅该子集）渲染。以中文
  撰写 4–6 条 bullet，覆盖：
  - 全球 FinTech 系统
  - 证券 market-data 基础设施
  - 金融图表与分析、技术指标计算
  - 策略分析与 backtesting
  - 交易所连接、多市场多时区跨地区金融产品系统
  不得引入 §5 中不存在的子项（不得添加具体交易所名称、公司客户等）。

- `## 工作模式`
  3–4 条 bullet，描述此 rule 激活后 agent 的协作模式：
  - 在工程域（代码评审、架构设计、平台/DevOps、harness/tool-boundary）
    以架构经验为背景，peer-level 协作，不解释用户已掌握的工程概念。
  - 在 market-data 域相关问题上以 §5 业务域为默认知识背景；不复述
    领域常识术语（tick / bar / OHLCV / session / symbol 等）。
  - 遇跨市场、跨时区、跨地区设计假设，按 §3.2 challenge-assumptions
    主动挑战，按 §3.1 structure-first 先列假设再给结论。
  - 不适用：纯教育对话（使用 /education 激活专项上下文）。

───────────────────────────────────────────────────────────────────────
VERBATIM LOCK（逐字锁定）
───────────────────────────────────────────────────────────────────────
本文件无逐字强制项；所有内容为 structured 派生。

───────────────────────────────────────────────────────────────────────
ANTI-PATTERNS（禁止出现于输出）
───────────────────────────────────────────────────────────────────────
1. 引入 spec §5 之外的内容（教育系统设计、§1 Identity 等）。
2. 捏造 SPEC 中不存在的事实：具体交易所、客户名、Pine Script、Go 引擎、
   特定经纪商或 SaaS 产品。
3. 添加 `paths:` frontmatter——本文件为 always-loaded，禁止 path-scoping。
4. 添加 spec 未指定的 H2 节。
5. 软化或解释化 §5 中的术语。
6. 用代码围栏包裹整个输出。
7. 使用文件工具（Read、Write、Edit）写入磁盘——必须直接输出到 stdout。

───────────────────────────────────────────────────────────────────────
SELF-CHECK（发出输出前在内部验证）
───────────────────────────────────────────────────────────────────────
- [ ] 输出**不含** YAML frontmatter（无 `---` 块、无 `paths:` 字段）。
- [ ] 仅含 3 个 H2 节，标题精确为：
      `## Architecture & Engineering`
      `## Market-Data 域上下文`
      `## 工作模式`
- [ ] `## Architecture & Engineering` 只覆盖 §5 Architecture & engineering 子集。
- [ ] `## Market-Data 域上下文` 只覆盖 §5 Business domain 子集。
- [ ] `## 工作模式` 含工程域 + market-data 域两个工作模式，以及 /education 排除说明。
- [ ] 总长度不超过 3500 字符。
- [ ] 输出中未出现 §5 教育系统设计、§6 Active Projects、§1/§3/§4/§7/§8 内容。
- [ ] 未使用任何文件工具——输出以纯文本形式打印到 stdout。

───────────────────────────────────────────────────────────────────────
OUTPUT（输出规则）
───────────────────────────────────────────────────────────────────────
仅输出最终文件内容。无前言。无解释。不得用 Markdown 代码围栏包裹整个输出。
直接以以下内容开头（无 frontmatter）：

    ## Architecture & Engineering

直接以 `## 工作模式` 的最后一条 bullet 结尾。文件正文之后不得输出任何文字。
