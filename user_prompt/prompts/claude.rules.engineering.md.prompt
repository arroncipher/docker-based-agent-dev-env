═══════════════════════════════════════════════════════════════════════
DERIVATION PROMPT · claude / user-level / rules/engineering.md
═══════════════════════════════════════════════════════════════════════

prompt-version: 1.0.0
spec-version-required: 1.0.6
target-file: ~/.claude/rules/engineering.md
intended-model: claude-sonnet-4-6 (or equivalent capability)
intended-temperature: 0

使用方法：
1. 打开 `../spec/arron.spec.md`，复制全部内容。
2. 将下方的 `<<<SPEC>>>` 替换为上述内容，得到完整 prompt。
3. 粘贴到全新 agent 会话并提交。
4. 将响应保存为 `../runs/<YYYY-MM-DD>_claude.rules.engineering/output.md`。

注意：实际写入路径为 `.coding_agents/claude_max20/.claude/rules/engineering.md`。
此文件为 path-scoped rule（含 `paths:` frontmatter），仅在 agent 尝试
读取匹配源码文件时按需加载。

═══════════════════════════════════════════════════════════════════════

你是一个确定性配置编译器。你的任务是将下方的用户 spec 中 §5（Domains）
的 Architecture & engineering 子集翻译为 path-scoped rule 文件。你只做
组合输出，不做解释、软化或信息增补。

───────────────────────────────────────────────────────────────────────
SPEC（逐字引用，不得修改）
───────────────────────────────────────────────────────────────────────
<<<SPEC>>>

───────────────────────────────────────────────────────────────────────
TARGET（输出目标）
───────────────────────────────────────────────────────────────────────
- file: ~/.claude/rules/engineering.md（Claude Code path-scoped rule）
- audience: Claude Code agent 在读到匹配源码文件时按需加载此文件
- style: 简洁 Markdown；叙述性内容以简体中文（zh-CN）；技术术语保留
  英文；不得加入任何引言
- length budget: 总字符数不超过 2000
- frontmatter: **必须**包含 `paths:` 字段；列出常见源码与 DevOps 文件
  扩展名 glob，按字母序：
    - `**/*.c` / `**/*.cpp` / `**/*.h` / `**/*.hpp`
    - `**/*.go`
    - `**/*.java` / `**/*.kt`
    - `**/*.js` / `**/*.jsx` / `**/*.ts` / `**/*.tsx`
    - `**/*.py`
    - `**/*.rs`
    - `**/*.sh`
    - `**/Dockerfile*`
    - `**/Makefile`
    - `**/*.tf` / `**/*.tfvars`
    - `**/docker-compose*.yml` / `**/docker-compose*.yaml`
- structure（按此顺序，使用以下精确 H2 标题）：
  1. `## Architecture & Engineering（按需上下文）`
  2. `## 适用判断`

各节内容规则：

- `## Architecture & Engineering（按需上下文）`
  基于 spec §5 中的 "Architecture & engineering" 子集（且仅该子集）
  渲染。以中文撰写 4–6 条 bullet，覆盖：
  - 单体 / 分布式 / cloud-native 经验
  - DevOps、平台工程、harness 工程
  - agentic 软件工程、host-agent、sandbox-executor、tool-boundary
  - AI Native 产品策略、workflow 重构
  - 开发者工具、可观测性、生产运维
  - 早期背景：嵌入式 + 图像处理算法优化
  允许将多个相关项合并为单一 bullet 以控制长度。不得引入 §5 中不存在
  的子项（例如不得添加 "Pine Script"、"Go 引擎" 等）。

- `## 适用判断`
  2–3 条 bullet，描述何时此 rule 与会话相关：
  - 工程域代码评审、架构设计、平台/DevOps 决策
  - 不适用：纯文档编辑、非工程对话

───────────────────────────────────────────────────────────────────────
VERBATIM LOCK（逐字锁定）
───────────────────────────────────────────────────────────────────────
本文件无逐字强制项；所有内容为 structured 派生。

───────────────────────────────────────────────────────────────────────
ANTI-PATTERNS（禁止出现于输出）
───────────────────────────────────────────────────────────────────────
1. 引入 spec §5 之外的内容（业务域、教育系统设计、§1 Identity 等）。
2. 捏造 SPEC 中不存在的事实：Pine Script / Go 引擎背景、设备名称、
   超出 §5 范围的具体工具或框架。
3. 缺少 YAML frontmatter（path-scoped rule 必须有 `paths:`）。
4. `paths:` 字段写成单一 glob 而不是多个 entry 的列表。
5. 添加 spec 未指定的 H2 节。
6. 软化或解释化 §5 中的术语（例如把 "sandbox-executor" 解释为通用名词）。
7. 用代码围栏包裹整个输出。
8. 使用文件工具（Read、Write、Edit）写入磁盘——必须直接输出到 stdout。

───────────────────────────────────────────────────────────────────────
SELF-CHECK（发出输出前在内部验证）
───────────────────────────────────────────────────────────────────────
- [ ] 输出以 YAML frontmatter（`---` 块）开头，含 `paths:` 字段。
- [ ] `paths:` 字段是 YAML list 形式（多行 `- ` 条目）。
- [ ] frontmatter 后是 `## Architecture & Engineering（按需上下文）` H2。
- [ ] 仅含 2 个 H2 节：`## Architecture & Engineering（按需上下文）`、
      `## 适用判断`。
- [ ] 总长度不超过 2000 字符。
- [ ] 输出中未出现 §5 业务域、教育系统设计、§1/§3/§4/§6/§7/§8 内容。
- [ ] 未使用任何文件工具——输出以纯文本形式打印到 stdout。

───────────────────────────────────────────────────────────────────────
OUTPUT（输出规则）
───────────────────────────────────────────────────────────────────────
仅输出最终文件内容。无前言。无解释。不得用 Markdown 代码围栏包裹整个输出。
直接以 YAML frontmatter 开头：

    ---
    paths:
      - "**/*.c"
      ...
    ---

    ## Architecture & Engineering（按需上下文）

直接以 `## 适用判断` 的最后一条 bullet 结尾。文件正文之后不得输出任何文字。
