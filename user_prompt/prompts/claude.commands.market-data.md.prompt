═══════════════════════════════════════════════════════════════════════
DERIVATION PROMPT · claude / user-level / commands/market-data.md
═══════════════════════════════════════════════════════════════════════

prompt-version: 1.0.0
spec-version-required: 1.0.6
target-file: ~/.claude/commands/market-data.md
status: RETIRED 2026-05-14 — Business domain merged into claude.rules.engineering (spec v1.0.10)
intended-model: claude-sonnet-4-6 (or equivalent capability)
intended-temperature: 0

使用方法：
1. 打开 `../spec/arron.spec.md`，复制全部内容。
2. 将下方的 `<<<SPEC>>>` 替换为上述内容，得到完整 prompt。
3. 粘贴到全新 agent 会话并提交。
4. 将响应保存为 `../runs/<YYYY-MM-DD>_claude.commands.market-data/output.md`。

注意：实际写入路径为 `.coding_agents/claude_max20/.claude/commands/market-data.md`。
此文件为 slash command（用户输入 `/market-data` 触发加载）。

═══════════════════════════════════════════════════════════════════════

你是一个确定性配置编译器。你的任务是将下方的用户 spec 中 §5（Domains）
的 Business domain 子集翻译为 slash command 文件。你只做组合输出，不做
解释、软化或信息增补。

───────────────────────────────────────────────────────────────────────
SPEC（逐字引用，不得修改）
───────────────────────────────────────────────────────────────────────
<<<SPEC>>>

───────────────────────────────────────────────────────────────────────
TARGET（输出目标）
───────────────────────────────────────────────────────────────────────
- file: ~/.claude/commands/market-data.md（Claude Code slash command）
- audience: Claude Code agent 在用户输入 `/market-data` 时加载此文件
- style: 简洁 Markdown；叙述性内容以简体中文（zh-CN）；技术术语保留
  英文（market-data、FinTech、tick、bar、backtesting 等）；不得加入引言
- length budget: 总字符数不超过 1500
- frontmatter: **必须**包含 `description:` 字段，描述命令用途
- structure（按此顺序，使用以下精确 H2 标题）：
  1. `## Market-Data 域上下文`
  2. `## 工作模式`

各节内容规则：

- `## Market-Data 域上下文`
  基于 spec §5 中的 "Business domain" 子集（且仅该子集）渲染。以中文
  撰写 4–6 条 bullet，覆盖：
  - 全球 FinTech 系统
  - 证券 market-data 基础设施
  - 金融图表与分析、技术指标计算
  - 策略分析与 backtesting
  - 交易所连接、多市场多时区跨地区金融产品系统
  不得引入 §5 中不存在的子项（例如不得添加 "Pine Script"、"Go 引擎"、
  具体交易所名称、具体公司客户）。

- `## 工作模式`
  2–3 条 bullet，描述命令激活后 agent 应采取的工作模式：
  - 在 market-data 域相关问题上以 §5 业务域为知识背景。
  - 优先用领域术语沟通（market-data、tick、bar、OHLCV、ladder、cross-region
    等），不解释 the user 已掌握的概念。
  - 遵循 §3 Meta-rules（peer-level / precision / challenge-assumptions）；
    遇跨市场或跨时区设计假设，按 §3.2 挑战。

───────────────────────────────────────────────────────────────────────
VERBATIM LOCK（逐字锁定）
───────────────────────────────────────────────────────────────────────
本文件无逐字强制项；所有内容为 structured 派生。

───────────────────────────────────────────────────────────────────────
ANTI-PATTERNS（禁止出现于输出）
───────────────────────────────────────────────────────────────────────
1. 引入 spec §5 业务域之外的内容（架构 & 工程、教育系统设计、§1 等）。
2. 捏造 SPEC 中不存在的事实：具体交易所、客户名、Pine Script、Go 引擎、
   特定经纪商或 SaaS 产品。
3. 缺少 YAML frontmatter `description:` 字段。
4. 添加 spec 未指定的 H2 节。
5. 解释 the user 已知的领域术语（market-data 不是 "市场数据"，
   tick 不是 "市场报价"——按 §3 peer-level 处理）。
6. 用代码围栏包裹整个输出。
7. 使用文件工具（Read、Write、Edit）写入磁盘。

───────────────────────────────────────────────────────────────────────
SELF-CHECK（发出输出前在内部验证）
───────────────────────────────────────────────────────────────────────
- [ ] 输出以 YAML frontmatter（`---` 块）开头，含 `description:` 字段。
- [ ] frontmatter 后是 `## Market-Data 域上下文` H2。
- [ ] 仅含 2 个 H2 节：`## Market-Data 域上下文`、`## 工作模式`。
- [ ] 总长度不超过 1500 字符。
- [ ] 输出中未出现 §5 架构 & 工程、教育系统设计、§1/§3/§4/§6/§7/§8 内容。
- [ ] 未使用任何文件工具——输出以纯文本形式打印到 stdout。

───────────────────────────────────────────────────────────────────────
OUTPUT（输出规则）
───────────────────────────────────────────────────────────────────────
仅输出最终文件内容。无前言。无解释。不得用 Markdown 代码围栏包裹整个输出。
直接以 YAML frontmatter 开头：

    ---
    description: ...
    ---

    ## Market-Data 域上下文

直接以 `## 工作模式` 的最后一条 bullet 结尾。文件正文之后不得输出任何文字。
