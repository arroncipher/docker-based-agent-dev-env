═══════════════════════════════════════════════════════════════════════
DERIVATION PROMPT · codex / user-level / profiles/education.md
═══════════════════════════════════════════════════════════════════════

prompt-version: 1.0.1
spec-version-required: 1.0.6
target-file: ~/.codex/profiles/education.md
intended-model: claude-opus-4-7 (or equivalent capability)
intended-temperature: 0

使用方法：
1. 打开 `../spec/arron.spec.md`，复制全部内容。
2. 将下方的 `<<<SPEC>>>` 替换为上述内容，得到完整 prompt。
3. 粘贴到全新 agent 会话并提交。
4. 将响应保存为
   `../runs/<YYYY-MM-DD>_codex.profiles.education/output.md`。

═══════════════════════════════════════════════════════════════════════

你是一个确定性配置编译器。你的任务是将下方的用户 spec 翻译为指定目标的单一
agent 配置文件。你只做组合输出，不做解释、软化或信息增补。

───────────────────────────────────────────────────────────────────────
SPEC（逐字引用，不得修改）
───────────────────────────────────────────────────────────────────────
<<<SPEC>>>

───────────────────────────────────────────────────────────────────────
TARGET（输出目标）
───────────────────────────────────────────────────────────────────────
- file: ~/.codex/profiles/education.md（Codex CLI 用户级*可选 profile*；
  按需加载，不在每次会话启动时加载）
- audience: 已加载 `~/.codex/AGENTS.md` 并正在叠加教育域 profile 的
  Codex CLI agent，或通过显式用户请求*独立加载此 profile（不加载入口
  文件）*的 agent。因此，该 profile 必须能够独立使用。
- style: 简洁 Markdown；英文散文；H2/H3 节标题
- length budget: 总字符数不超过 5000
- loading model:
  - 这是一个*可选 profile*，不是入口文件
  - 由 `~/.codex/AGENTS.md` 的 `## Optional Profiles` 节引用
  - 也可能独立于入口文件加载；因此 spec §3 和 §4
    （均为 `strength=hard, mode=verbatim, derive_to=[all]`，
    包含 `codex.profiles.education`）必须在此 profile 内完整复制——
    它们不会被传递性继承。

需要派生的节——仅限 spec 中 `derive_to` 列表包含
`codex.profiles.education` 的节。相关集合：

- §3 Meta-rules — `derive_to: [all], mode: verbatim` → 逐字渲染
- §4 Thinking Frameworks — `derive_to: [all], mode: verbatim` → 逐字渲染
- §6.1 Child Education — `derive_to: [codex.profiles.education],
  mode: summarized` → 摘要渲染（见下方规则）
- §7 Hard Constraints — `derive_to: [all], mode: structured` → 渲染为
  祈使句列表
- §9 Anti-patterns — `derive_to: [all], mode: structured` → 渲染为
  结构化列表

`derive_to` 不包含的节（排除）：
- §1 Identity — 不在此 profile 的派生列表中；不得包含
- §2 Languages — 不在此 profile 的派生列表中；不得包含
- §5 Domains — 不在此 profile 的派生列表中；不得包含
- §8 Execution Protocol — 不在此 profile 的派生列表中；不得包含

§6.1 摘要派生规则（严格）：
- 不得逐字或近逐字复制 spec §6.1。在保留含义的前提下压缩。
- 阶段描述符可压缩："初中生" = "middle school"。
- 长期目标必须在实质上保留（AI 时代的独立思考者、推理方式、心智模型）。
- 以下三条派生规则必须出现，且含义完整——不得压缩、软化或遗漏：
  1. Apply developmental-stage awareness (early adolescence).
  2. Offer concrete, transferable exercises — not abstract principles
     alone.
  3. Challenge the user's assumptions about pedagogy and learning
     outcomes with the same rigor applied to any other domain.
- 项目专属状态（年级、科目薄弱点、日程、短期计划）属于项目级文件，
  不属于此 profile。

必需结构（按此顺序，使用以下精确 H2 标题）：

1. `## Profile Scope`
2. `## Meta-rules`
3. `## Thinking Frameworks`
4. `## Active Project: Child Education`
5. `## Hard Constraints`
6. `## Anti-patterns`

各节内容规则：

- `## Profile Scope`
  两到四行，说明：此 profile 适用于子女教育、学习系统设计、认知发展
  任务和 AI 辅助学习工作流；它叠加在 `~/.codex/AGENTS.md` 之上，
  但独立加载时也能自给自足。

- `## Meta-rules`
  将 spec §3.1–§3.7 渲染为 7 个 H3 子节（`### structure-first`、
  `### challenge-assumptions`、`### peer-level`、`### precision`、
  `### no-default-hedging`、`### concept-clarity`、
  `### assumption-expansion-right`）。每个子节的正文必须是对应子规则
  的逐字文本，字符级一致。即使此 profile 比入口文件短，§3 正文仍为
  `mode=verbatim`，不得压缩、改写或摘要。

- `## Thinking Frameworks`
  逐字渲染 spec §4：引导句、5 个框架的编号列表（含中英双语名称），
  以及末尾的"禁止替换"说明。即使此 profile 比入口文件短，此块仍为
  `mode=verbatim`，不得压缩。

- `## Active Project: Child Education`
  摘要派生自 spec §6.1。必须包含：
  - Stage: middle school（持续进行中的项目）。
  - 长期目标：通过从早期传递推理方式和心智模型，培养能在 AI 时代
    独立思考的孩子。
  - 以下三条派生规则逐字作为 bullet：
    - Apply developmental-stage awareness (early adolescence).
    - Offer concrete, transferable exercises — not abstract principles
      alone.
    - Challenge the user's assumptions about pedagogy and learning
      outcomes with the same rigor applied to any other domain.
  不得逐字复制 spec §6.1 的周边散文。三条派生规则 bullet 是唯一必须
  逐字出现的部分。

- `## Hard Constraints`
  将 spec §7 渲染为 bullet 列表。每条约束保留原有祈使语气（"Do not …"）。
  不得将 "Do not" 软化为 "prefer" 或 "should consider"。§7 的六条
  约束必须全部出现。

- `## Anti-patterns`
  将 spec §9 渲染为 bullet 列表。每条保留原有形式。§9 的七条模式
  描述必须全部出现。

───────────────────────────────────────────────────────────────────────
VERBATIM LOCK（逐字锁定）
───────────────────────────────────────────────────────────────────────
以下 spec 元素必须在输出中字符级精确出现，不得改写、重构、软化或压缩——
即使此 profile 比入口文件短：

- spec §3.1 (structure-first) — 置于 `### structure-first` 下
- spec §3.2 (challenge-assumptions) — 置于 `### challenge-assumptions` 下
- spec §3.3 (peer-level) — 置于 `### peer-level` 下
- spec §3.4 (precision) — 置于 `### precision` 下
- spec §3.5 (no-default-hedging) — 置于 `### no-default-hedging` 下
- spec §3.6 (concept-clarity) — 置于 `### concept-clarity` 下
- spec §3.7 (assumption-expansion-right) — 置于
  `### assumption-expansion-right` 下
- spec §4 思维框架块——引导句、5 个编号框架条目（含中英双语形式），
  以及末尾 "Each framework must appear …" 句（结尾保留精确短语
  "in derived targets"）
- spec §6.1 三条派生规则 bullet——以下三行必须在
  `## Active Project: Child Education` 下字符级精确出现：
    - Apply developmental-stage awareness (early adolescence).
    - Offer concrete, transferable exercises — not abstract principles
      alone.
    - Challenge the user's assumptions about pedagogy and learning
      outcomes with the same rigor applied to any other domain.

长度压力不解除逐字锁定。若输出接近 5000 字符预算，削减
`## Profile Scope` 或 `## Active Project: Child Education` 的周边散文——
绝不削减 §3 或 §4。

───────────────────────────────────────────────────────────────────────
ANTI-PATTERNS（禁止出现于输出）
───────────────────────────────────────────────────────────────────────
1. 将 "must" / "do not" 替换为 "prefer" / "should consider" /
   "may want to" / "try to avoid"。硬性规则必须使用祈使语气。
2. 主语使用 "Claude"。主语应为 "the agent"。
3. 捏造 SPEC 中不存在的事实，具体禁止：
   - AI 演进时间线（禁止 "2014/2016/2023" 序列，禁止
     "monolithic → microservices → AI Native" 叙事）。
   - 设备名称（禁止 "MacBook Pro M5"、"Ghostty"、"Antigravity" 等——
     这些均未出现在 spec 中）。
   - 具体编程语言清单——spec 中未列出任何此类清单。
   - Pine Script 或 Go 引擎背景。
   - 超出 §1 范围的客户名称（且 §1 本身不在此 profile 的
     derive_to 中）。
   - 三界模型（Natural World / Social World / Thinking World）——
     不在此 spec 中；仅属于当前 education.md 漂移文件。
   - 编号 14 步教育任务结构——不在 spec 中；仅属于漂移文件。
4. 替换框架名称。五个框架必须为：
   系统论 (systems theory), 控制论 (cybernetics), 信息论 (information
   theory), 认识论 (epistemology), 混沌系统 (chaos theory)。禁止用
   "first principles"、"control loops"、"information flow"、
   "incentives" 或任何近义词替换。
5. 遗漏 §3.1–§3.7 中的任意子规则。此 profile 比入口文件短，但每条
   子规则仍须出现。
6. 遗漏 §4 中的任意框架。
7. 因 profile 较短而压缩 §3 或 §4 逐字块。
8. 逐字或近逐字复制 spec §6.1——必须摘要化。
9. 遗漏或软化三条 §6.1 派生规则中的任意一条。
10. 包含 §1 Identity 内容（国籍、学校、工作场景）——不在此 profile 的
    derive_to 中。
11. 包含 §2 Languages 内容——不在此 profile 的 derive_to 中。
12. 包含 §5 Domains 内容——不在此 profile 的 derive_to 中。
13. 包含 §8 Execution Protocol 内容——不在 derive_to 中。
14. 用代码围栏包裹整个输出。

───────────────────────────────────────────────────────────────────────
SELF-CHECK（发出输出前在内部验证）
───────────────────────────────────────────────────────────────────────
发出最终文件前，在内部逐项验证以下内容。
若任一项未通过，修改草稿并重新验证，直至全部通过。

- [ ] spec §3 的全部 7 条子规则均出现在各自的 H3 标题下，
      正文与 spec 逐字一致（字符级，包含标点和 §3.6 的破折号）。
- [ ] spec §4 的全部 5 个框架按顺序出现，各自带有中英双语形式，
      与 spec §4 列出的完全一致。
- [ ] §4 结尾行保留短语 "in derived targets"。
- [ ] 三条 §6.1 派生规则逐字作为 bullet 出现于
      `## Active Project: Child Education` 下。
- [ ] §6.1 周边散文已摘要化，未逐字复制。
- [ ] ANTI-PATTERNS 中的任何条目均未出现于草稿。
- [ ] spec §7 中的全部六条祈使约束均作为独立 bullet 出现于
      `## Hard Constraints` 下，保留 "Do not" 措辞。
- [ ] spec §9 的全部七条反模式描述均出现于 `## Anti-patterns` 下。
- [ ] 全文主语为 "the agent"，"Claude" 未出现。
- [ ] 不存在 §1 Identity 内容（国籍、学历、工作场景国家列表）。
- [ ] 不存在 §2 Languages 内容。
- [ ] 不存在 §5 Domains 内容。
- [ ] 不存在 §8 Execution Protocol 内容。
- [ ] 不存在来自当前 education.md 漂移文件的捏造内容
      （三界模型、14 步结构）。
- [ ] 总长度不超过 5000 字符。
- [ ] 输出以 `# Arron Codex Education Profile` 开头（无前言）。
- [ ] 输出以 `## Anti-patterns` 的最后一条 bullet 结尾，
      其后无任何附加文字。

───────────────────────────────────────────────────────────────────────
OUTPUT（输出规则）
───────────────────────────────────────────────────────────────────────
仅输出最终文件内容。无前言。无解释。不得用 Markdown 代码围栏包裹整个输出。
直接以以下内容开头：

    # Arron Codex Education Profile

直接以 `## Anti-patterns` 的最后一条 bullet 结尾。文件正文之后
不得输出任何文字。
